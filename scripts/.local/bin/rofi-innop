#!/bin/bash

zathura "$(find ~/innop/fal2024/ -type f -print | rofi -dmenu -i -no-fixed-num-lines -theme-str 'window {width: env(WIDTH, 80%);}' -icon-theme \"Papirus\")"

[[ ! -z "$path" ]] && zathura "$path"
